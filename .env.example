# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_KEY=<your-api-key-or-leave-empty-for-managed-identity>
AZURE_OPENAI_API_VERSION=2024-02-15-preview
AZURE_OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# SerpAPI for real job data (optional - uses mock data if not set)
SERPAPI_API_KEY=<your-serpapi-key-optional>

# PostgreSQL Database (optional - uses in-memory storage if not set)
# For Azure Database for PostgreSQL Flexible Server:
DB_HOST=your-server.postgres.database.azure.com
DB_PORT=5432
DB_NAME=job_agent
DB_USER=your_admin_username
DB_PASSWORD=your_password
DB_SSL_MODE=require

# ---- OpenTelemetry / Observability (optional) ----
# Agent Framework emits traces, metrics, and logs via OpenTelemetry.
# Set ENABLE_INSTRUMENTATION=true to activate.
#
# For local dev with Aspire Dashboard (docker):
#   docker run --rm -d -p 18888:18888 -p 4317:18889 --name aspire mcr.microsoft.com/dotnet/aspire-dashboard:latest
#   Then open http://localhost:18888 to view traces.
ENABLE_INSTRUMENTATION=false
ENABLE_SENSITIVE_DATA=false
ENABLE_CONSOLE_EXPORTERS=false
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
# OTEL_SERVICE_NAME=job-agent

# For Azure Monitor / Application Insights (production):
# pip install azure-monitor-opentelemetry
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...